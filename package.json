{
  "name": "sharepoint-loader",
  "version": "0.4.0",
  "private": true,
  "description": "A standalone Manifest V3 extension for loading full SharePoint lists.",
  "scripts": {
    "check": "node scripts/validate.mjs && npm test",
    "build": "node scripts/build.mjs",
    "store-assets": "python3 scripts/generate-store-assets.py",
    "test": "node --test test/*.test.js",
    "icons": "node scripts/generate-icons.mjs"
  }
}
